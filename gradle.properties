org.gradle.jvmargs = -Xmx3G

# Mod Information
mod_id = versionchaser
mod_name = Version Chaser
mod_version = 1.0

# Maven Information
maven_group = surreal
archives_base_name = VersionChaser

# If any properties changes below this line, run `gradlew setupDecompWorkspace` and refresh gradle again to ensure everything is working correctly.

# Access Transformers
# <modid>_at.cfg
use_access_transformer = true

# Coremod
include_mod = true
coremod_plugin = surreal.versionchaser.core.VCModPlugin
